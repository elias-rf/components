import { NavbarApp } from '@/client/features/navbar-app.jsx'

const meta = {
  component: NavbarApp,
}

export default meta

export const Default = {
  render: () => {
    return (
      <>
        <NavbarApp />
      </>
    )
  },
}
