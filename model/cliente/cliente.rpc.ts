import type { TClienteRpc } from "./cliente.type";
//#region import
//#endregion

export function clienteRpcFactory({
  clienteModel,
}: {
  clienteModel: TClienteRpc;
}) {
  //#region def
  //#endregion

  return clienteModel;
}
